---
import Filters from "./index.astro";

const { years } = Astro.props;

let yearsRange = years.map((year) => {
  return {
    value: year,
    label: year,
  };
});
---

<Filters name="year" values={yearsRange} title="Filtrar por años" />
