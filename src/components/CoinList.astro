---
import Coin from "./Coin";
const { coinContainerId, coins } = Astro.props;
---

<ul
  id={coinContainerId}
  class="grid sm:grid-cols-3 lg:grid-cols-6 grid-cols-2 gap-8 items-center justify-center"
>
  {coins.map((coin) => <Coin client:visible {...coin} />)}
</ul>
